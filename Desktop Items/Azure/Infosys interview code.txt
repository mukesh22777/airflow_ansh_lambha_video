PySpark Syntax for Aggrgate Sum , Max and Min 



spark =SparkSession


result = df.agg(
sum("value").alias("total_sum")
max()
min()
)


PySpark Syntax for OverBy Partition and fetch the 3rd Max salary 

Employee Table 
EmpId, EmpName, Salary , Dept  -- 

100 M  3000 "K"
100 M  2000 "K"
100 O 1500 "K". -->O/P
100 L 1000 "K"

columns = ["EmpId","EmpName","Salary","Dept"]
df = spark.createDataFrame(data,columns)

window_spec = Window.partitionBy("Dept".orderBy(col("Salary").desc())

df_rn = df.withColumn("row_num",row_number().over(window_spec))

result = 


order_detail table:
customer_id,line_item_id, order_id, item_id, qty, price,order_date

Figure out the last_purchase_date and first_purchase_date of customer 

df = spark.createDataFrame(data,columns)

df = df.withColumn("order_date",df["order_date"].cast("date"))


result = df.groupBy("customer_id").agg(
        min("order_date").alias("first_pur_date")
        max("order_date").alias("last_pur_date")
        )


Figure out the 3rd_last_purchase_date and 2nd_last_purchase_date of customer 



What is Memory Spill and Disk Spill?


1- Can You Explain What a List and Dict Comprehension Is?


s= [x** for x in range(1,6)]

s_d = {x:x**2 for x in range(1,6)}}


When Would You Use a List vs. a Tuple vs. a Set in Python?

what is Python’s Parameter-Passing Mechanism ?


Input: nums = [-7,0,3,5,9,12], target = 3
Output: 2
def search(nums, target):
    try:
        return nums.index(target)
    except ValueError:
        return -1
        
      
Merge & Sort 
Input: list1 = [1,2,4], list2 = [1,3,4]
Output: [1,1,2,3,4,4]

- timestamp 
- DateColumn - UTC TimeZOne

val dfwithUTC = df.withColumn("timestamp",to_utc_timestamp(col(timestamp),originalTimeZoone))
